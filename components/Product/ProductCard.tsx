import { Product } from '@/utils/ProductInterface'
import React from 'react'
interface InputFieldsProps {
  Product: Product
}
const ProductCard: React.FC<InputFieldsProps> = ({ Product }) => {
  return <div></div>
}

export default ProductCard
